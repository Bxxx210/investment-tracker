export default function StocksPage() {
  return (
    <section className="rounded-[2rem] border border-white/10 bg-white/5 p-8 text-slate-100 shadow-2xl shadow-cyan-950/20 backdrop-blur-xl">
      <p className="text-xs uppercase tracking-[0.3em] text-cyan-300/80">
        Stocks
      </p>
      <h3 className="mt-3 text-3xl font-semibold text-white">
        หน้าหุ้นกำลังเตรียมใช้งาน
      </h3>
      <p className="mt-4 max-w-2xl text-sm leading-7 text-slate-300">
        เมนูนี้ถูกวางโครงไว้แล้วเพื่อให้ navigation สมบูรณ์ ต่อไปค่อยเติมฟอร์มและ list สำหรับซื้อขายหุ้น
      </p>
    </section>
  );
}
